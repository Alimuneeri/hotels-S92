# Gemfile
source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# This is a development dependency for running the server locally with `bundle exec jekyll serve`
group :jekyll_plugins do
  gem "webrick", "~> 1.8"
  gem "jekyll-feed", "~> 0.17.0" # <-- ADD THIS LINE
end